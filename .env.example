# Copy to .env (gitignored). Format: KEY=value, no quotes.
GCP_PROJECT_ID=encoders-sandbox-rodo
BQ_LOCATION=US

# Token used by Terraform to store the GitHub PAT in Secret Manager (see README, step 3).
# TF_VAR_github_token=github_pat_xxxxxxxx
